# phpstorm project files
/.idea

# vscode project files
/.vscode

# windows thumbnail cache
Thumbs.db

# composer
/vendor/
composer.phar

# Mac DS_Store Files
.DS_Store

# ignores the cache files
.php-cs-fixer.cache
/.phpunit.cache

# env files
.env
# composer local
composer.local.json
composer.local.lock

# Docker files
# (compose.override.yml is intentionally TRACKED — deploy.sh pins -f compose.yml
# so it is never merged on the server, but every developer gets the same setup)
/docker/nginx/ssl/

# Docker data volumes
docker/data/

# Node.js
/node_modules/

# Vite build output
/public/assets/
/public/.vite/

# Vite hot file (local dev only)
/public/hot

# Windows NTFS Zone.Identifier files
*:Zone.Identifier

# Temp files
/public/favicon-code.html

# opencode (local AI agent config — kept out of the repo)
/AGENTS.md
/.opencode/